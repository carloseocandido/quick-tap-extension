{
  "name": "quick-tap-extension",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/popup.ts --bundle --format=iife --outfile=dist/popup.js",
    "build:package": "npm run package:zip",
    "package:zip": "esbuild scripts/package.ts --bundle --platform=node --format=esm --target=node16 --outfile=.tmp/package.mjs && node .tmp/package.mjs",
    "watch": "esbuild src/popup.ts --bundle --format=iife --outfile=dist/popup.js --watch",
    "lint": "eslint .",
    "format": "prettier . --write"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/chrome": "^0.1.37",
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.3",
    "eslint": "^10.0.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.0"
  }
}
